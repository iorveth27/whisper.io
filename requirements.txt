# Core dependencies
pyaudio>=0.2.14
pynput>=1.7.6
pyperclip>=1.9.0
numpy>=1.26.0
pyyaml>=6.0
huggingface-hub>=0.20.0
pyobjc-framework-Quartz>=10.0

# MLX backend (Apple Silicon) — enabled by default
mlx-whisper>=0.4.0

# faster-whisper backend (NVIDIA/CPU) — uncomment to use instead of MLX
# faster-whisper>=1.0.0
